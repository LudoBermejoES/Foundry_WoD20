{{!-- v3 Effects tab — SUPERSEDED, add-pc-sheet-v3 §8.3.

      This was the FIRST part migrated (see this file's own git history for the original header:
      chosen first because it had nothing at stake — 38 lines, zero floats, four actions, empty on
      most characters). It is no longer registered in `PCActorSheetV3.PARTS`, and no entry in
      `tabs` points at it: Effects now renders as a SUB-TAB inside Ajustes/Settings instead,
      through `templates/actor/parts/settings.hbs`'s `effectsinsettings`-gated body.

      Kept on disk and still registered in `module/templates.js`, wrapping the SAME content via
      the include below (not a frozen copy that can drift from it) — the reference for what a
      standalone Effects tab used to look like, the same way `v3/bio.hbs` stays after §8.1/§8.2
      retired it into Features. Nothing renders this file today. --}}
<section class="tab {{tab.cssClass}}" data-tab="{{tab.id}}" data-group="{{tab.group}}" data-drop-area="true">
	{{> "systems/worldofdarkness/templates/actor/v3/effects_body.hbs"}}
</section>
