<div class="sheet-headline sheet-banner-small splatFont"><span class="sheet-banner-text">{{localize 'wod.health.health'}}</span></div>	
<!--<div>Vänsterklick att lägga till skada, högerklick rensar aktuell ruta.</div>-->

<div class="sheet-boxcontainer">
    <div>{{getInjuredLevel actor.type actor.system.health actor.system.health.damage}}</div>
</div>
    
<div class="sheet-boxcontainer">

    {{#if (gt actor.system.health.bruised.total 0)}}

        <div class="health">
            <div class="resource-counter healthbox" data-name="data.system.health.bruised">
                {{#numLoop actor.system.health.bruised.total}}            
                    <span class="resource-value-step healthBox bruised" 
                        data-type="{{../config.sheettype.mortal}}"
                        data-action="editHealth"
                        data-index="{{this}}" 
                        data-state="{{damageState ../actor.system.health.bruised.label ../health this}}"></span>            
                {{/numLoop}}
            </div>
        </div>

    {{/if}}  
    
    {{#if (gt actor.system.health.hurt.total 0)}}

        <div class="health">
            <div class="resource-counter healthbox"  data-name="data.system.health.hurt">
                {{#numLoop actor.system.health.hurt.total}}            
                    <span class="resource-value-step healthBox hurt" 
                        data-type="{{../config.sheettype.mortal}}"
                        data-action="editHealth"
                        data-index="{{this}}" 
                        data-state="{{damageState ../actor.system.health.hurt.label ../health this}}"></span>            
                {{/numLoop}}
            </div>
        </div>

    {{/if}}

    {{#if (gt actor.system.health.injured.total 0)}}

        <div class="health">
            <div class="resource-counter healthbox" data-name="data.system.health.injured">
                {{#numLoop actor.system.health.injured.total}}            
                    <span class="resource-value-step healthBox injured" 
                        data-type="{{../config.sheettype.mortal}}"
                        data-action="editHealth"
                        data-index="{{this}}" 
                        data-state="{{damageState ../actor.system.health.injured.label ../health this}}"></span>            
                {{/numLoop}}
            </div>
        </div>

    {{/if}}

    {{#if (gt actor.system.health.wounded.total 0)}}

        <div class="health">
            <div class="resource-counter healthbox" data-name="data.system.health.wounded">
                {{#numLoop actor.system.health.wounded.total}}            
                    <span class="resource-value-step healthBox wounded" 

                        data-action="editHealth"
                        data-index="{{this}}" 
                        data-state="{{damageState ../actor.system.health.wounded.label ../health this}}"></span>            
                {{/numLoop}}
            </div>
        </div>

    {{/if}}

    {{#if (gt actor.system.health.mauled.total 0)}}

        <div class="health">
            <div class="resource-counter healthbox" data-name="data.system.health.mauled">
                {{#numLoop actor.system.health.mauled.total}}            
                    <span class="resource-value-step healthBox mauled" 
                        data-type="{{../config.sheettype.mortal}}"
                        data-action="editHealth"
                        data-index="{{this}}" 
                        data-state="{{damageState ../actor.system.health.mauled.label ../health this}}"></span>            
                {{/numLoop}}
            </div>
        </div>

    {{/if}}

    {{#if (gt actor.system.health.crippled.total 0)}}

        <div class="health">
            <div class="resource-counter healthbox" data-name="data.system.health.crippled">
                {{#numLoop actor.system.health.crippled.total}}            
                    <span class="resource-value-step healthBox crippled" 
                        data-type="{{../config.sheettype.mortal}}"
                        data-action="editHealth"
                        data-index="{{this}}" 
                        data-state="{{damageState ../actor.system.health.crippled.label ../health this}}"></span>            
                {{/numLoop}}
            </div>
        </div>

    {{/if}}

    {{#if (gt actor.system.health.incapacitated.total 0)}}

        <div class="health">
            <div class="resource-counter healthbox" data-name="data.system.health.incapacitated">
                {{#numLoop actor.system.health.incapacitated.total}}            
                    <span class="resource-value-step healthBox incapacitated" 
                        data-type="{{../config.sheettype.mortal}}"
                        data-action="editHealth"
                        data-index="{{this}}" 
                        data-state="{{damageState ../actor.system.health.incapacitated.label ../health this}}"></span>            
                {{/numLoop}}
            </div>
        </div>

    {{/if}}
</div>

{{#if actor.system.settings.usechimerical}}

    <div class="sheet-headline sheet-banner-small splatFont"><span class="sheet-banner-text">{{localize 'wod.health.chimerical'}}</span></div>

    <div class="sheet-boxcontainer">
        <div>{{getInjuredLevel actor.type actor.system.health actor.system.health.damage.chimerical}}</div>
    </div>

    <div class="sheet-boxcontainer">

        {{#if (gt actor.system.health.bruised.total 0)}}

            <div class="health">
                <div class="resource-counter healthbox" data-name="data.system.health.bruised">
                    {{#numLoop actor.system.health.bruised.total}}            
                        <span class="resource-value-step healthBox bruised" 
                            data-type="{{../config.sheettype.changeling}}"
                            data-action="editHealth"
                            data-index="{{this}}" 
                            data-state="{{damageState ../actor.system.health.bruised.label ../chimericalhealth this}}"></span>            
                    {{/numLoop}}
                </div>
            </div>

        {{/if}}  
        
        {{#if (gt actor.system.health.hurt.total 0)}}

            <div class="health">
                <div class="resource-counter healthbox" data-name="data.system.health.hurt">
                    {{#numLoop actor.system.health.hurt.total}}            
                        <span class="resource-value-step healthBox hurt" 
                            data-type="{{../config.sheettype.changeling}}"
                            data-action="editHealth"
                            data-index="{{this}}" 
                            data-state="{{damageState ../actor.system.health.hurt.label ../chimericalhealth this}}"></span>            
                    {{/numLoop}}
                </div>
            </div>

        {{/if}}

        {{#if (gt actor.system.health.injured.total 0)}}

            <div class="health">
                <div class="resource-counter healthbox" data-name="data.system.health.injured">
                    {{#numLoop actor.system.health.injured.total}}            
                        <span class="resource-value-step healthBox injured" 
                            data-type="{{../config.sheettype.changeling}}"
                            data-action="editHealth"
                            data-index="{{this}}" 
                            data-state="{{damageState ../actor.system.health.injured.label ../chimericalhealth this}}"></span>            
                    {{/numLoop}}
                </div>
            </div>

        {{/if}}

        {{#if (gt actor.system.health.wounded.total 0)}}

            <div class="health">
                <div class="resource-counter healthbox" data-name="data.system.health.wounded">
                    {{#numLoop actor.system.health.wounded.total}}            
                        <span class="resource-value-step healthBox wounded" 
                            data-type="{{../config.sheettype.changeling}}"
                            data-action="editHealth"
                            data-index="{{this}}" 
                            data-state="{{damageState ../actor.system.health.wounded.label ../chimericalhealth this}}"></span>            
                    {{/numLoop}}
                </div>
            </div>

        {{/if}}

        {{#if (gt actor.system.health.mauled.total 0)}}

            <div class="health">
                <div class="resource-counter healthbox" data-name="data.system.health.mauled">
                    {{#numLoop actor.system.health.mauled.total}}            
                        <span class="resource-value-step healthBox mauled" 
                            data-type="{{../config.sheettype.changeling}}"
                            data-action="editHealth"
                            data-index="{{this}}" 
                            data-state="{{damageState ../actor.system.health.mauled.label ../chimericalhealth this}}"></span>            
                    {{/numLoop}}
                </div>
            </div>

        {{/if}}

        {{#if (gt actor.system.health.crippled.total 0)}}

            <div class="health">
                <div class="resource-counter healthbox" data-name="data.system.health.crippled">
                    {{#numLoop actor.system.health.crippled.total}}            
                        <span class="resource-value-step healthBox crippled" 
                            data-type="{{../config.sheettype.changeling}}"
                            data-action="editHealth"
                            data-index="{{this}}" 
                            data-state="{{damageState ../actor.system.health.crippled.label ../chimericalhealth this}}"></span>            
                    {{/numLoop}}
                </div>
            </div>

        {{/if}}

        {{#if (gt actor.system.health.incapacitated.total 0)}}

            <div class="health">
                <div class="resource-counter healthbox" data-name="data.system.health.incapacitated">
                    {{#numLoop actor.system.health.incapacitated.total}}            
                        <span class="resource-value-step healthBox incapacitated" 
                            data-type="{{../config.sheettype.changeling}}"
                            data-action="editHealth"
                            data-index="{{this}}" 
                            data-state="{{damageState ../actor.system.health.incapacitated.label ../chimericalhealth this}}"></span>            
                    {{/numLoop}}
                </div>
            </div>

        {{/if}}
    </div>

{{/if}}

{{!--
    add-wraith-pc-splat §3.3 — the Corpus track.

    Deliberately NOT a second copy of the health ladder above, and the rules are explicit about why: a
    wraith "pierde Corpus EN LUGAR DE niveles de Salud a razón de uno por uno"
    (wraith20-el-olvido-nsr-es L6811). So there are no wound-level names, and there is **no penalty line** —
    a wraith takes no dice or movement penalty from Corpus damage at all (L10581).

    The track is as long as PERMANENT Corpus (L10543: permanent is the maximum, temporary fluctuates and
    can never exceed it), which is exactly the length of the array `calculateHealth`'s wraith branch
    returns — every entry with `label: ""`.

    The three marks are the book's own ("/" bashing, "x" lethal, "*" aggravated; bashing escalates by
    marking a second line into an X, L10611-10613). They matter for WHICH STATE a full track puts the
    wraith in, not for penalties: all bashing -> Vacilante (L10577); lethal -> Atormentado and an immediate
    Harrowing (L10583). Neither is automated — both are Storyteller calls with a Willpower-vs-Angst roll.
    Healing is 1 Pathos for 2 bashing or 1 lethal, max one Pathos per turn (L10635).
--}}
{{#if corpushealth}}

    <div class="sheet-headline sheet-banner-small splatFont"><span class="sheet-banner-text">{{localize 'wod.advantages.corpus'}}</span></div>

    <div class="sheet-boxcontainer">
        <div class="health">
            <div class="resource-counter healthbox" data-name="data.system.health.damage.corpus">
                {{#numLoop corpushealth.length}}
                    <span class="resource-value-step healthBox"
                        data-type="{{../config.sheettype.wraith}}"
                        data-action="editHealth"
                        data-index="{{this}}"
                        data-state="{{damageState "" ../corpushealth this}}"></span>
                {{/numLoop}}
            </div>
        </div>
    </div>

{{/if}}

<div class="sheet-boxcontainer">
    <div class="information-area">{{localize "wod.health.penalty"}}: {{ignorePain actor.system.health.damage.woundpenalty}}</div>    
</div>